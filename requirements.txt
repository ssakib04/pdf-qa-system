PyPDF2==3.0.1             # For extracting text from PDFs
sentence-transformers==2.2.2  # For embedding models like all-MiniLM-L6-v2
scikit-learn==1.3.0       # For cosine similarity, evaluation metrics
numpy==1.25.2             # Numerical operations
faiss-cpu==1.7.4          # Vector index for similarity search
tk==0.1.0                 # For basic Tkinter GUI (use system tkinter if on macOS/Linux)
tqdm==4.66.1              # For progress bars (optional but useful)
transformers==4.35.2      # If using any Hugging Face models directly (optional)
torch==2.1.0              # Required by sentence-transformersa
